terminal:
npx create-react-app nom-de-votre-application
cd nom-de-votre-application
npm start
on modifie le contenu de fichier index.js
ajout des composant:
	//MonComposant.js
import React from 'react';

const MonComposant = () => {
  return (
    <div>
      <h1>Bienvenue dans MonComposant!</h1>
    </div>
  );
}

export default MonComposant;
on utilise export composant pour pouvoir importer ce composant dans un autre fichier

JSX:HTML intégré dans du code JavaScript.

html tag tabda b lowercase 
component yabda b capital letter(ili fih des html tags)
Never define a component inside another component!
(n declari composant w baad ken nheb nistaamlou fi wost composant eekher nhel tag ctt)


export default function Button() {}  => import Button from './Button.js'; (default)
export function Button() {}   => import { Button } from './Button.js'; (named)


